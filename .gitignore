# Compiled binary
/llamacpp-launcher

# Project agent manual (root-level, kept out of version control)
/AGENTS.md
*.exe
*.out

# Build / run artifacts
*.log

# Local scratch
/dist
/node_modules
/tmp/

# Agent workspace (plan, changelog, manual) — never commit
/opencode/
